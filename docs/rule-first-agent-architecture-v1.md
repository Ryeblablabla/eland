# Eland 规则优先人物架构 v1

状态：当前权威设计。本文覆盖其他文档中关于人物决策频率、模型调用时机、模型额度、计划失败处理和月内执行粒度的旧描述。

关联：[月度时间模型](./monthly-time-model-v1.md) · [空间行动契约](./spatial-action-contract-v1.md) · [最小物质—人物—动作模型](../three-body/design/minimal-emergent-human-model.md) · [像素世界（空间·历史·迁移）](./pixel-world-v1.md) · [文明演化双闭环与实验协议](./evolution-iteration-loop-v1.md)

## 1. 核心决定

Eland 是一个可以独立运行的确定性社会模拟游戏。大模型不是世界时钟、人物行动或规则裁决的依赖。

旧假设：

> 人物出现关键决策机会，就应尽量交给模型。

当前假设：

> 人物始终由本地规划器获得可执行决定；只有少数长期战略、玩家交互和创造性时刻，才异步邀请模型提出建议或补充表达。

因此必须满足：

- 没有 API Key、模型超时、模型返回非法内容或服务完全离线时，文明仍能从开局连续演化到终局；
- 模型调用不得位于 `advanceMonth`、规划刻度或动作提交的等待链上；
- 模型输出只能成为待验证的建议、人物表达或叙事，不直接成为世界事实；
- 快速演化的耗时主要由本地规则复杂度决定，不随人口 × 月份线性增加外部请求；
- 相同种子、相同玩家输入和相同已接受模型建议仍可确定性回放。

## 2. 三层权威

```text
世界层（唯一事实权威）
  物质、空间、身体、资源、关系、承诺、知识、时间和后果

本地人物层（行动权威）
  反射、目标组合、效用评分、计划编译、动作预演、局部修复

模型层（非权威增强）
  长期战略建议、玩家对话、创造性提案、人物口吻和历史叙事
```

世界层和本地人物层必须独立闭环。模型层可以缺席。

## 3. 时间：每月 15 个规划刻度

“月”仍是存档、人口、身体状态、环境过程、历史索引和 UI 日历的最小权威时间单位，但不再是人物规划与执行的最小单位。

```text
1 月 = 15 个 planning tick
```

每个规划刻度代表本月可辨认的一段行动机会。它不是固定天数，也不向 UI 承诺现实日历；15 只是当前动作密度与性能之间的规则常数。

月度流程：

```text
月初环境与协议生命周期
→ planning tick 1
→ planning tick 2
→ ...
→ planning tick 15
→ 月末身体、状态、人口和观察器结算
→ 提交 MonthRecord / MonthFrame
```

每个 tick 对每位仍可行动人物执行：

1. 读取该 tick 开始时的局部感知与身体状态；
2. 优先执行吃、喝、逃离直接危险和依赖幼儿照护等确定性反射；
3. 检查当前长期意图是否完成、失效、受阻或需要局部修复；
4. 若无可执行意图，本地规划器立即选择一个合法目标；
5. 把目标编译为下一项原子动作，并在提交前预演其前置条件；
6. 先用廉价规则补移动、材料、恢复或替代目标；
7. 执行至多一个原子动作，写入真实路径、成本、结果和来源；
8. 下一人物读取已经提交的当前世界；稳定执行顺序写入历史。

人物不必每个 tick 更换长期目标。3～12 月状态目标仍可持续，但引擎每个 tick 都有机会推进、修复或在客观失效时改换手段。

## 4. 本地规划器

本地规划器不是占位 Mock，而是正式游戏系统。它的输入只有人物可获得的事实：

- 当前身体、状态和行动限制；
- 眼前可见对象与有来源的记忆目标；
- 当前长期意图及最近成功/失败；
- 人格偏置、关系、承诺和共同体职责；
- 引擎生成的合法可供性；
- 时间、距离、材料和风险的粗粒度估计。

它维护四个目标分支：

```text
survival    生存、健康与直接安全
production  采集、储备、建造、耕作、制作与维护
social      照护、合作、交换、承诺、冲突与共同体
inquiry     探索、观察、试验、学习、记录与表达
```

目标分支不是四个同时执行的计划。人物在任一 tick 只有一个当前执行焦点；其他分支保存需求压力、未完成目标和最近进展。本地选择分数至少考虑：

```text
score = 紧急度
      + 对长期目标的贡献
      + 未完成生产与空闲奖励
      + 人格、关系和承诺偏置
      + 等待时间与失败后的替代价值
      - 风险、距离、材料缺口和重复社交惩罚
      - 稳定计划的无意义改换惩罚
```

开局不调用模型。所有先民在第一个 planning tick 由本地规划器获得首个可执行意图。

## 5. 计划编译与动作模拟

模型或人物理由都不能直接引用临时 `optionId` 作为长期语义。长期目标使用稳定的结构化谓词，例如：

```ts
interface GoalSpec {
  domain: 'survival' | 'production' | 'social' | 'inquiry';
  predicate: FactPredicate;
  target?: WorldRef;
  reviewAtMonth?: number;
  sourceFactIds: string[];
}
```

本地计划编译器把目标逐步展开为五种原子动作。提交前的 Action Simulator 只使用世界规则进行短视窗预演：

- 目标是否仍存在且本人能够知道；
- 是否可达、是否需要先移动；
- 材料、工具、授权、身体和空间是否满足；
- 当前动作是否会产生负库存、越界、远程作用或其他非法状态；
- 是否需要先吃喝、恢复、取材或选择已知替代方法。

动作模拟器不预测完整社会未来，也不让模型判断合法性。它只保证眼前计划可执行并能留下明确失败原因。

## 6. 分级修复

失败处理固定为：

```text
一级：局部规则修复
  重算路径、靠近目标、补材料、恢复身体、重新选择同目标的已知方法

二级：本地重新规划
  保留长期目标，替换当前子目标；或在目标客观失效时选择其他目标分支

三级：异步战略建议
  仅在连续失败、长期停滞或重大世界变化后创建模型任务
```

一级和二级当 tick 完成，不等待网络。三级结果以后到达时，只能在下一规划刻度重新验证；已经过时或不合法的建议被丢弃并保留审计记录。

## 7. 模型的三个入口

实现状态：后端当前已经落地“已发生事实的叙事增强旁车”，接口与持久化格式见[非阻塞叙事增强旁车](../three-body/design/non-authoritative-model-enhancements.md)。长期战略、玩家交互和创造性提案仍是架构入口，尚未接入后端长程演化主链；在它们完成同样的来源绑定、异步返回和本地重验之前，不得把设计中的接口当作已经实现的能力。

### 7.1 长期战略

触发示例：文明年度复盘、连续多月停滞、首次遭遇重大灾害、新产业链瓶颈、共同体规则危机。模型返回稳定语义的 `StrategySuggestion`，不得返回原子动作 ID、路径或世界补丁。

### 7.2 玩家交互

玩家主动打开对话、提出建议或要求人物解释时，可以等待模型生成交互文本。等待只阻塞这次交互，不阻塞世界时钟。玩家指令进入人物记忆或候选目标后，仍由本地规划器与规则验证。

### 7.3 创造性与叙事

模型可以提出名称、故事、符号、仪式、艺术表达、假说或新协议草案，也可以为已经发生的重大事件补充人物视角。接受、传播和客观后果必须由真实 `communicate / attend / act` 及后续历史形成。

## 8. 异步模型任务

```ts
interface ModelTask {
  id: string;
  kind: 'strategy' | 'interaction' | 'creative' | 'narrative';
  requestedAtMonth: number;
  requestedAtPlanningTick: number;
  subjectIds: string[];
  sourceFactIds: string[];
  status: 'queued' | 'running' | 'completed' | 'failed' | 'stale';
}

interface StrategySuggestion {
  taskId: string;
  subjectId: string;
  goal: GoalSpec;
  reason: string;
  expression?: string;
}
```

模型任务有以下硬边界：

- 后端先提交规则结果，再派发任务；
- 同一任务可重试，但重试不暂停模拟；
- 结果必须重新读取当前状态验证，不能使用请求时的临时 option ID；
- 建议只有被人物本地规划器接受后才产生新的 DecisionFact；
- 纯叙事结果进入投影或史册，不修改世界；
- 任务失败是可观察的基础设施状态，不是文明事件。

## 9. 模型预算

删除“每 N 人月一次模型决策”的概念。模型预算改为低频任务预算：

- 战略：默认每个文明年 1～3 个任务，重大转折可额外触发；
- 交互：由玩家行为和产品额度控制；
- 创造性：只在人物已有余量且出现真实表达/试验机会时触发；
- 叙事：优先批量处理重大事件，快速演化可在完成后统一补写。

紧急生存、必须回应和履约不享有“无限模型豁免”，它们由本地规则立即执行。模型只能事后补充解释。

## 10. 记忆

短期记忆保存近期动作、失败、对话和承诺，用于局部修复。长期人格保存由多次事实支持的价值、习惯、关系和长期目标偏置。

```text
单次事件 → 短期记忆
重复且重要的事实 → 本地规则形成稳定偏置候选
可选模型 → 把证据压缩成人物可读表达
规则验证 → 更新长期人格投影
```

模型可以改写表达，不能在没有事件证据时直接修改人格权重或关系余额。

生活对话使用同样的证据链。本地规则先从身体处境、实际照护、共同劳动、失败、发现和共同养育事件生成带 `sourceFactIds` 的定向开场，被点名者再引用该开场作出支持或保留回应。只有两轮都通过参与者、来源和去重验证的事件，才能更新关系并成为后续结伴/生殖提议的关系证据。模型可改写口吻，但不得更换话题、参与者或事实来源。

近亲繁衍也遵循同一原则。世界规则不把直系或共亲关系写成“禁止生殖”的非法动作，而是根据亲缘距离与双亲已有遗传负荷，确定后代的遗传易感性、初始身体和能力偏差、寿命压力及后续疾病概率。人物只有在亲眼经历体弱出生、反复疾病或能力衰退后，才形成带来源的短期记忆；多次证据把它提升为“近亲后代风险较高”的个人认识。该认识进入本地规划评分，使人物倾向拒绝近亲提议、优先无亲缘伴侣，并可通过真实沟通传播，最终才可能涌现为亲族禁忌或婚配规范。模型不能预先注入这项知识。

## 11. 历史与回放

MonthRecord 除月度事实外，应记录 15 个 tick 的执行顺序和动作引用。规则决策是权威历史；模型任务和建议是独立审计流。

回放：

- 不重新运行本地规划器；
- 不重新调用模型；
- 应用已提交的事件和补丁；
- 分岔后从新的 branchId 继续本地规划；
- 已完成但尚未被接受的模型建议不自动跨分支生效。

文明演化程度使用《文明指数 v1》从人口、认知疆域/功能基础设施、科技、社会和历史五类事实派生。指数是观察层，不能反向为人物解锁能力。恒/乱纪元、天气、预言、脱水休眠和动物生态遵循《纪元、预言与生态 v1》，它们必须产生真实状态与后果，不只生成叙事。

## 12. 前后端迁移边界

本轮以后端长程演化为主：

- 后端 `/evolve` 和批量模拟必须使用规则优先主链；
- 前端单步与自动演化已使用本地规划主链；Kimi 兼容代码保留，后续只能通过非阻塞模型任务重新接入；
- 前端旧路径不得被后端快速演化复用；
- 无论前端是否迁移，领域层都必须始终提供本地规划入口。

## 13. 多运行演化验证

规则修改不得只依据一次运行判断有效或无效。验证应按《文明演化双闭环与实验协议》选择与因果时标匹配的实验矩阵：多个配对种子、10/20/30 年中的一个或多个时长，必要时增加重复或更长的跨代运行。

本地规则完全确定时，相同种子重复只用于验证可回放性，不能冒充独立样本。基线与候选必须使用相同种子、时长、配置和模型策略；结论同时读取聚合分布、发生率、配对差异和代表性历史。单次运行只能暴露候选问题或提供因果案例。

## 14. 验收不变量

- 关闭网络和模型配置后，可从新文明推进至少 1200 月；
- 每个完整月份恰有 15 个规划刻度；
- 空闲且可行动的人不会因为没有模型额度长期保持无意图；
- 紧急反射、履约和必须回应不调用模型；
- 模型任务超时或返回非法内容时，已经推进的月份不回滚、不停顿；
- 模型不能直接返回或持久化临时 option ID 作为长期战略；
- 每项客观变化都能追溯到规则动作和世界事件；
- 任意 10/20/30 年及更长快速演化的主循环不等待任何模型请求；
- 可在模拟完成后异步生成少量真实模型叙事，而不改变确定性历史。

## 15. 文档优先级

发生冲突时按以下顺序解释：

1. 本文的规则优先、15 tick 和异步模型边界；
2. `evolution-iteration-loop-v1.md` 的演化闭环与实验判断方法；
3. `civilization-index-v1.md` 与 `three-body-era-ecology-v1.md` 的指数和环境规则；
4. `minimal-emergent-human-model.md` 与 `emergent-capability-substrate-v1.md` 的物质、动作和社会事实；
5. `monthly-time-model-v1.md` 与 `spatial-action-contract-v1.md` 的时间和执行契约；
6. `pixel-world-v1.md` 中仍适用于当前 schema 的空间、历史和迁移原则；
7. README 只描述当前可运行接口，不覆盖领域设计。
